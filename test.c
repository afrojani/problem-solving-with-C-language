#include <stdio.h>

int main()
{
    int a, b;
    printf("enter two values: ");
    scanf("%d%d", &a, &b);
    printf("the value of a is %d\n", a);
    printf("the value of b is %d", b);
    return 0;
}